# EMARX Argument Depth Protocol

Use this file before drafting and again before delivery. It exists to stop EMARX from producing papers that are formally compliant but intellectually weak.

## Core Failure This Protocol Blocks

A draft can have 10,000 characters, second-level headings, Word output, and ordered references, yet still fail. It fails when it is only a formatted expansion of a checklist.

Typical failure signs:

- the central thesis is generic and repeated rather than developed;
- headings are complete but each section reads like a task description;
- citations appear as "某研究指出" support labels rather than digested scholarship;
- the paper repeats a few abstract terms without building new distinctions;
- paragraphs explain the obvious instead of advancing a problem;
- prose sounds like rule compliance, not mature academic thought.

## Argument Permission

Do not draft a full paper until these items are answered:

```text
central thesis:
why this thesis is not obvious:
research object:
concept boundary:
theoretical tension:
mechanism chain:
literature position:
section obligations:
likely weak spots:
```

Reject argument permission if:

- the thesis is only "technology brings opportunities and challenges, so governance is needed";
- the problem can be answered by common sense;
- the section plan merely repeats the title;
- the paper has no concept that changes meaning through the argument;
- the mechanism chain is only a list of factors.

## Literature Digestion

A source must be digested before it is cited. Build this for every important source:

```text
source:
problem it answers:
core concept or mechanism:
usable insight:
limitation or blind spot:
how this paper uses it: inherit / refine / contrast / extend / correct
target section:
target sentence function:
```

Do not cite a source just because it is related. A citation must perform an argumentative function:

- establish a concept;
- supply a mechanism;
- expose a limitation;
- support a contrast;
- provide a case or material;
- mark a scholarly position that this paper advances beyond.

Weak citation patterns:

- "相关研究指出..." followed by a compressed summary;
- one citation per paragraph as decoration;
- citation after a generic sentence that could be written without the source;
- using a source to prove only that the topic exists.

## Section Obligation

Each first-level section must carry a distinct obligation:

```text
Section 1: clarify the object and create conceptual pressure.
Section 2: show what changes structurally and why it matters.
Section 3: explain how risks are generated, not merely name them.
Section 4: build paths that answer the diagnosed mechanisms.
Conclusion: return to the thesis at a higher level.
```

Each second-level heading must do more than divide content. It must state the specific analytical action:

- boundary;
- mechanism;
- contradiction;
- transformation;
- mediation;
- consequence;
- governance response;
- theoretical return.

## Paragraph Argument Unit

A mature paragraph is not a miniature explanation of a heading. It is an argument unit.

Preferred paragraph unit:

```text
claim -> mechanism -> source/material -> counter-pressure -> judgment landing
```

Other valid units:

```text
concept distinction -> consequence -> thesis return
source insight -> limitation -> this paper's advance
phenomenon -> structural cause -> governance implication
policy term -> theoretical relation -> practical boundary
```

Reject a paragraph if it only does one of these:

- defines a term already obvious from the title;
- lists benefits or risks;
- repeats the same abstract nouns;
- says "should strengthen / improve / optimize" without explaining why this path answers a diagnosed mechanism;
- ends with a slogan instead of a judgment.

## Bad Draft Rejection

Before delivery, ask:

```text
Is this merely a longer version of an outline?
Could each section be summarized by its heading without losing much?
Are the same concepts repeated instead of transformed?
Do citations enter the argument or merely decorate it?
Does the paper contain a sentence that a mature scholar would recognize as an actual insight?
Does the conclusion return to a sharpened thesis, or merely restate the path list?
```

If the answer exposes a formatted but hollow draft, do not deliver. Rewrite the thesis and sections first.

## Negative Example Lesson

The file `生成式人工智能语境下中华文化国际传播的机遇、挑战与路径-v5重写版.md` was closer to v5 format but still failed as mature academic writing. It had length, second-level headings, references, and Word output, yet its prose remained formulaic. The lesson is not "add more requirements"; the lesson is that formal compliance must be subordinated to argument quality.
